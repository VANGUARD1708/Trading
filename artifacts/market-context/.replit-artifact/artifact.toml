kind = "web"
previewPath = "/"
title = "Market Context Platform"
version = "1.0.0"
id = "artifacts/market-context"
router = "path"

[[integratedSkills]]
name = "react-vite"
version = "1.0.0"

[[services]]
name = "web"
paths = [ "/" ]
localPort = 20626

[services.development]
run = "pnpm --filter @workspace/market-context run dev"

[services.production]
build = [ "pnpm", "--filter", "@workspace/market-context", "run", "build" ]
publicDir = "artifacts/market-context/dist/public"
serve = "static"

[[services.production.rewrites]]
from = "/*"
to = "/index.html"

[services.env]
PORT = "20626"
BASE_PATH = "/"
